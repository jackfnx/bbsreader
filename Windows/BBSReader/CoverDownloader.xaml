<Window x:Class="BBSReader.CoverDownloader"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BBSReader"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignFont}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Title="Cover Downloader" Height="650" Width="1200" Loaded="Window_Loaded">
    <DockPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" DockPanel.Dock="Bottom">
            <Button x:Name="Accept" Content=" Accept " Margin="20" Click="Accept_Click"></Button>
            <Button x:Name="Cancel" Content=" Cancel " Margin="20" Click="Cancel_Click"></Button>
        </StackPanel>
        <TextBox DockPanel.Dock="Top" Margin="20" x:Name="CoverUrl" />
        <Image x:Name="CoverImg" Width="160" Height="200" />
    </DockPanel>
</Window>
